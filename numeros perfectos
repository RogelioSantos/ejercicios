def es_perfecto(n):
    suma = 0
    for i in range(1, n):
        if n % i == 0:
            suma = suma + i
    return suma == n

numero = 28  # Valor variable a gusto propio
if es_perfecto(numero):
    print(numero, "es un número perfecto")
else:
    print(numero, "no es un número perfecto")
